<%= form_for(message) do |f| %>
  <%= f.collection_select :to, Contact.all, :phone, :name%>
  <%= f.hidden_field :from, :value => 7155982908  %>
  <%= f.label :body %>
  <%= f.text_field :body %>
  <%= f.submit %>
<% end %>
